// Standard libraries
#include <stdio.h>
#include <stdlib.h>

// Header file we define
#include "header.h"

void top_n_webpages(int N, double *scores, int n)
{
    double *top_list = calloc(n, sizeof(int));
    // for (int i = 0; i<n; i++) 
    // {
    //     for (int j = 0; j<n)
    // }

    free(top_list);
}